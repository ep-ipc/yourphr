# CLAUDE.md

**Canonical agent brief:** [`AGENTS.md`](AGENTS.md).

- **Session protocol** (kit-managed): the block between `KIT:START` / `KIT:END` in `AGENTS.md`.
- **Product context** (this repo): everything in `AGENTS.md` **below** `KIT:END` — mission, PHI rules, commands, architecture, deploy, conventions.

This file exists so Claude Code and other tools that auto-load `CLAUDE.md` still land on guidance. **Do not** maintain a second copy of architecture or PHI rules here; edit `AGENTS.md` below `KIT:END` only.
